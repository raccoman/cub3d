#ifndef UTILS_H
#define UTILS_H

# include <math.h>

# define FALSE 0
# define TRUE 1

# define MAX(a, b) a > b ? a : b
# define MIN(a, b) a > b ? b : a
# define CAP(max, min, v) v > max ? (max) : (min > v ? min : v)

#endif
